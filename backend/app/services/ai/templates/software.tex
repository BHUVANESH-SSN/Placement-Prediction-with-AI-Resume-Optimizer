\documentclass[a4paper,10pt]{article}

% Packages
\usepackage{latexsym}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage{marvosym}
\usepackage{graphicx}
\usepackage[usenames,dvipsnames]{color}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{fancyhdr}
\usepackage{tabularx}

% Theme color
\definecolor{primary}{HTML}{\VAR{theme_primary_color|default('2D3748', true)}}

% Page settings
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% Margins
\addtolength{\oddsidemargin}{-0.5in}
\addtolength{\evensidemargin}{-0.5in}
\addtolength{\textwidth}{1in}
\addtolength{\topmargin}{-0.6in}
\addtolength{\textheight}{1.2in}

\urlstyle{same}
\raggedbottom
\raggedright
\setlength{\tabcolsep}{0in}

% Section formatting
\titleformat{\section}{
  \vspace{\VAR{section_vspace|default('-8pt', true)}}\scshape\raggedright\large\bfseries
}{}{0em}{}[\color{primary}\titlerule\vspace{-6pt}]

% Custom commands
\newcommand{\resumeItem}[1]{
  \item\small{#1 \vspace{-2pt}}
}

\newcommand{\resumeSubheading}[4]{
  \vspace{-1pt}\item
    \begin{tabular*}{0.97\textwidth}[t]{l@{\extracolsep{\fill}}r}
      \textbf{#1} & \textbf{#2} \\
      \textit{\small#3} & \textit{\small #4} \\
    \end{tabular*}\vspace{-5pt}
}

\newcommand{\resumeProjectHeading}[2]{
  \vspace{-1pt}\item
    \begin{tabular*}{0.97\textwidth}{l@{\extracolsep{\fill}}r}
      \textbf{#1} & \textit{\small#2} \\
    \end{tabular*}\vspace{-5pt}
}

\renewcommand\labelitemii{$\vcenter{\hbox{\tiny$\bullet$}}$}

\newcommand{\resumeSubHeadingListStart}{\begin{itemize}[leftmargin=0.15in, label={}]}
\newcommand{\resumeSubHeadingListEnd}{\end{itemize}}
\newcommand{\resumeItemListStart}{\begin{itemize}[leftmargin=0.2in]}
\newcommand{\resumeItemListEnd}{\end{itemize}\vspace{-5pt}}

\begin{document}

%---------- HEADER ----------
\BLOCK{if photo}
\begin{minipage}[t]{0.75\textwidth}
  \begin{center}
    {\Huge\bfseries\scshape \VAR{name}} \\[8pt]
    \small
    \BLOCK{if email}\href{mailto:\VAR{email}}{\VAR{email}}\BLOCK{endif}%
    \BLOCK{if phone} $|$ \VAR{phone}\BLOCK{endif}%
    \BLOCK{if linkedin} $|$ \href{\VAR{linkedin}}{LinkedIn}\BLOCK{endif}%
    \BLOCK{if github} $|$ \href{\VAR{github}}{GitHub}\BLOCK{endif}%
    \BLOCK{if website} $|$ \href{\VAR{website}}{Portfolio}\BLOCK{endif}
  \end{center}
\end{minipage}%
\begin{minipage}[t]{0.25\textwidth}
  \raggedleft
  \vspace{-20pt}
  \includegraphics[width=2.5cm,height=3cm,keepaspectratio]{\VAR{photo}}
\end{minipage}
\BLOCK{else}
\begin{center}
  {\Huge\bfseries\scshape \VAR{name}} \\[8pt]
  \small
  \BLOCK{if email}\href{mailto:\VAR{email}}{\VAR{email}}\BLOCK{endif}%
  \BLOCK{if phone} $|$ \VAR{phone}\BLOCK{endif}%
  \BLOCK{if linkedin} $|$ \href{\VAR{linkedin}}{LinkedIn}\BLOCK{endif}%
  \BLOCK{if github} $|$ \href{\VAR{github}}{GitHub}\BLOCK{endif}%
  \BLOCK{if website} $|$ \href{\VAR{website}}{Portfolio}\BLOCK{endif}
\end{center}
\BLOCK{endif}

\vspace{8pt}

%---------- PROFESSIONAL SUMMARY ----------
\BLOCK{if professional_summary}
\section{Professional Summary}
\small{\VAR{professional_summary}}
\vspace{6pt}
\BLOCK{endif}

%---------- EDUCATION ----------
\BLOCK{if education}
\section{Education}
\resumeSubHeadingListStart
\BLOCK{for edu in education}
  \resumeSubheading
    {\VAR{edu.degree}\BLOCK{if edu.branch} \VAR{edu.branch}\BLOCK{endif}}{\BLOCK{if edu.start_year}\VAR{edu.start_year}\BLOCK{endif}\BLOCK{if edu.start_year and edu.end_year} -- \BLOCK{endif}\BLOCK{if edu.end_year}\VAR{edu.end_year}\BLOCK{endif}}
    {\BLOCK{if edu.institution}\VAR{edu.institution}\BLOCK{elif edu.college}\VAR{edu.college}\BLOCK{endif}}{\BLOCK{if edu.cgpa}CGPA: \VAR{edu.cgpa}\BLOCK{endif}}
\BLOCK{endfor}
\resumeSubHeadingListEnd
\vspace{4pt}
\BLOCK{endif}

%---------- SKILLS ----------
\BLOCK{if skills_categorized or skills}
\section{Technical Skills}
\small{
\BLOCK{if skills_categorized}
  \BLOCK{for category, skill_list in skills_categorized.items()}
  \textbf{\VAR{category}:} \BLOCK{for skill in skill_list}\VAR{skill}\BLOCK{if not loop.last}, \BLOCK{endif}\BLOCK{endfor} \\
  \BLOCK{endfor}
\BLOCK{else}
  \textbf{Technologies:} \BLOCK{for skill in skills}\VAR{skill}\BLOCK{if not loop.last}, \BLOCK{endif}\BLOCK{endfor}
\BLOCK{endif}
}
\vspace{6pt}
\BLOCK{endif}

%---------- PROFESSIONAL EXPERIENCE ----------
\BLOCK{if experience}
\section{Professional Experience}
\resumeSubHeadingListStart
\BLOCK{for exp in experience}
  \resumeSubheading
    {\VAR{exp.role}}{\BLOCK{if exp.start_date}\VAR{exp.start_date}\BLOCK{endif}\BLOCK{if exp.start_date and exp.end_date} -- \BLOCK{endif}\BLOCK{if exp.end_date}\VAR{exp.end_date}\BLOCK{elif exp.start_date}Present\BLOCK{endif}}
    {\BLOCK{if exp.company}\VAR{exp.company}\BLOCK{endif}}{\BLOCK{if exp.location}\VAR{exp.location}\BLOCK{endif}}
    \BLOCK{if exp.description}
    \resumeItemListStart
      \BLOCK{if exp.description is string}
      \resumeItem{\VAR{exp.description}}
      \BLOCK{else}
      \BLOCK{for item in exp.description}
      \resumeItem{\VAR{item}}
      \BLOCK{endfor}
      \BLOCK{endif}
    \resumeItemListEnd
    \BLOCK{endif}
\BLOCK{endfor}
\resumeSubHeadingListEnd
\vspace{4pt}
\BLOCK{endif}

%---------- PROJECTS ----------
\BLOCK{if projects}
\section{Projects}
\resumeSubHeadingListStart
\BLOCK{for proj in projects}
  \resumeProjectHeading
    {\BLOCK{if proj.github_link}\href{\VAR{proj.github_link}}{\VAR{proj.title}}\BLOCK{elif proj.live_link}\href{\VAR{proj.live_link}}{\VAR{proj.title}}\BLOCK{else}\VAR{proj.title}\BLOCK{endif}\BLOCK{if proj.tech_stack} $|$ \textit{\small \BLOCK{for tech in proj.tech_stack}\VAR{tech}\BLOCK{if not loop.last}, \BLOCK{endif}\BLOCK{endfor}}\BLOCK{endif}}{}
    \BLOCK{if proj.description}
    \resumeItemListStart
      \BLOCK{if proj.description is string}
      \resumeItem{\VAR{proj.description}}
      \BLOCK{else}
      \BLOCK{for item in proj.description}
      \resumeItem{\VAR{item}}
      \BLOCK{endfor}
      \BLOCK{endif}
    \resumeItemListEnd
    \BLOCK{endif}
\BLOCK{endfor}
\resumeSubHeadingListEnd
\vspace{4pt}
\BLOCK{endif}


%---------- CERTIFICATIONS ----------
\BLOCK{if certifications}
\section{Certifications}
\resumeSubHeadingListStart
\BLOCK{for cert in certifications}
  \item \small{%
    \BLOCK{if cert.link}\href{\VAR{cert.link}}{\textbf{\BLOCK{if cert.name}\VAR{cert.name}\BLOCK{elif cert.title}\VAR{cert.title}\BLOCK{else}Certification\BLOCK{endif}}}\BLOCK{else}\textbf{\BLOCK{if cert.name}\VAR{cert.name}\BLOCK{elif cert.title}\VAR{cert.title}\BLOCK{else}Certification\BLOCK{endif}}\BLOCK{endif}%
    \BLOCK{if cert.issuer} -- \VAR{cert.issuer}\BLOCK{endif}%
    \BLOCK{if cert.issue_date} (\VAR{cert.issue_date})\BLOCK{elif cert.date} (\VAR{cert.date})\BLOCK{endif}%
  }
\BLOCK{endfor}
\resumeSubHeadingListEnd
\vspace{4pt}
\BLOCK{endif}

%---------- ACHIEVEMENTS ----------
\BLOCK{if achievements}
\section{Achievements \& Awards}
\resumeSubHeadingListStart
\BLOCK{for ach in achievements}
  \BLOCK{if ach is string}
  \item \small{\VAR{ach}}
  \BLOCK{else}
  \item \small{\BLOCK{if ach.title}\textbf{\VAR{ach.title}}\BLOCK{endif}\BLOCK{if ach.description} -- \VAR{ach.description}\BLOCK{endif}}
  \BLOCK{endif}
\BLOCK{endfor}
\resumeSubHeadingListEnd
\vspace{4pt}
\BLOCK{endif}

%---------- CAREER OBJECTIVE (always at bottom) ----------
\BLOCK{if career_objective or objective}
\section{Career Objective}
\small{\BLOCK{if career_objective}\VAR{career_objective}\BLOCK{else}\VAR{objective}\BLOCK{endif}}
\vspace{6pt}
\BLOCK{endif}

\end{document}