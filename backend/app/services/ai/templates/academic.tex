%% ------
%% academic.tex  --- Academic / Research Resume Template
%%
%% Layout: Header ? Professional Summary ? Skills ?
%%         Publications ? Experience ? Projects ?
%%         Education ? Certifications ? Achievements ?
%%         Research Interests ? Career Objective
%% ------
\documentclass[10pt, letterpaper]{article}

\usepackage[top=0.45in,bottom=0.45in,left=0.55in,right=0.55in]{geometry}
\usepackage{fontenc}
\usepackage{inputenc}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{tabularx}
\usepackage{xcolor}

\definecolor{primarycolor}{HTML}{\VAR{theme_primary_color}}
\definecolor{accentcolor}{HTML}{\VAR{theme_accent_color}}

\hypersetup{colorlinks=true,urlcolor=accentcolor,linkcolor=accentcolor,pdfborder={0 0 0}}

\titleformat{\section}
  {\large\bfseries\color{primarycolor}}{}{0em}{}
  [\vspace{-4pt}\rule{\linewidth}{0.8pt}\vspace{\VAR{section_vspace}}]
\titlespacing*{\section}{0pt}{8pt}{4pt}

\setlist[itemize]{leftmargin=1.2em,itemsep=\VAR{bullet_vspace},parsep=0pt,topsep=2pt}
\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

\begin{document}

% ------ HEADER ------
\begin{center}
  {\LARGE\bfseries \VAR{name}} \\[4pt]
  \small
  \BLOCK{ if phone }\VAR{phone}\BLOCK{ endif }%
  \BLOCK{ if phone and email } \textbar\ \BLOCK{ endif }%
  \BLOCK{ if email }\href{mailto:\VAR{email}}{\VAR{email}}\BLOCK{ endif }%
  \BLOCK{ if email and linkedin } \textbar\ \BLOCK{ endif }%
  \BLOCK{ if linkedin }\href{https://linkedin.com/in/\VAR{linkedin}}{linkedin.com/in/\VAR{linkedin}}\BLOCK{ endif }%
  \BLOCK{ if linkedin and github } \textbar\ \BLOCK{ endif }%
  \BLOCK{ if github }\href{https://github.com/\VAR{github}}{github.com/\VAR{github}}\BLOCK{ endif }%
\end{center}
\vspace{\VAR{header_vspace}}

% ------ PROFESSIONAL SUMMARY ------
\BLOCK{ if professional_summary }
\section{Professional Summary}
\small\VAR{professional_summary}
\vspace{\VAR{post_section_vspace}}
\BLOCK{ endif }

% ------ EDUCATION ------
\BLOCK{ if education }
\section{Education}
\BLOCK{ for edu in education }
\noindent
\begin{tabularx}{\linewidth}{@{}X r@{}}
  \textbf{%
    \VAR{edu.degree}%
    \BLOCK{ if edu.branch } \VAR{edu.branch}\BLOCK{ endif }%
  } &
  \textit{%
    \BLOCK{ if edu.start_year }\VAR{edu.start_year}\BLOCK{ endif }%
    \BLOCK{ if edu.start_year and edu.end_year } -- \BLOCK{ endif }%
    \BLOCK{ if edu.end_year }\VAR{edu.end_year}\BLOCK{ endif }%
  } \\
  \small \VAR{edu.institution} & \\
\BLOCK{ if edu.cgpa }
  \small CGPA: \VAR{edu.cgpa} & \\
\BLOCK{ endif }

% ------ RESEARCH INTERESTS ------
\BLOCK{ if research_interests }
\section{Research Interests}
\small\VAR{research_interests}
\vspace{\VAR{post_section_vspace}}
\BLOCK{ endif }

% ------ SKILLS ------
\BLOCK{ if skills_categorized }
\section{Skills}
\begin{tabular}{@{}p{2.2cm} l@{}}
\BLOCK{ for category, skill_list in skills_categorized.items() }
  \textbf{\VAR{category}:} & \VAR{skill_list | join(', ')} \\[2pt]
\BLOCK{ endfor }
\end{tabular}
\vspace{\VAR{post_section_vspace}}
\BLOCK{ elif skills }
\section{Skills}
\small \VAR{skills | join(' \textbar\ ')}
\vspace{\VAR{post_section_vspace}}
\BLOCK{ endif }

% ------ PUBLICATIONS ------
\BLOCK{ if publications }
\section{Publications}
\begin{itemize}
\BLOCK{ for pub in publications }
  \item \small
    \BLOCK{ if pub.title }\textbf{\VAR{pub.title}}\BLOCK{ endif }%
    \BLOCK{ if pub.venue }\ --- \textit{\VAR{pub.venue}}\BLOCK{ endif }%
    \BLOCK{ if pub.year }\ (\VAR{pub.year})\BLOCK{ endif }%
    \BLOCK{ if pub.link }\ \href{\VAR{pub.link}}{\footnotesize[PDF]}\BLOCK{ endif }%
\BLOCK{ endfor }
\end{itemize}
\vspace{\VAR{post_section_vspace}}
\BLOCK{ endif }

% ------ EXPERIENCE ------
\BLOCK{ if experience }
\section{Experience}
\BLOCK{ for exp in experience }
\noindent
\begin{tabularx}{\linewidth}{@{}X r@{}}
  \textbf{\VAR{exp.role}} & \textit{\small \BLOCK{ if exp.start_date }\VAR{exp.start_date}\BLOCK{ endif }\BLOCK{ if exp.start_date and exp.end_date } -- \BLOCK{ endif }\BLOCK{ if exp.end_date }\VAR{exp.end_date}\BLOCK{ else }Present\BLOCK{ endif }} \\
  \textit{\small \VAR{exp.company}} & \\
\end{tabularx}
\vspace{2pt}
\BLOCK{ if exp.description }
\begin{itemize}
\BLOCK{ if exp.description is string }
  \item \small \VAR{exp.description}
\BLOCK{ else }
\BLOCK{ for bullet in exp.description }
  \item \small \VAR{bullet}
\BLOCK{ endfor }
\BLOCK{ endif }
\end{itemize}
\BLOCK{ endif }
\vspace{\VAR{item_vspace}}
\BLOCK{ endfor }
\vspace{\VAR{post_section_vspace}}
\BLOCK{ endif }

% ------ PROJECTS ------
\BLOCK{ if projects }
\section{Projects}
\BLOCK{ for proj in projects }
\noindent
\begin{tabularx}{\linewidth}{@{}X r@{}}
  \textbf{\VAR{proj.title}}%
  \BLOCK{ if proj.github_link }\ \href{\VAR{proj.github_link}}{\small[GitHub]}\BLOCK{ endif }%
  \BLOCK{ if proj.live_link }\ \href{\VAR{proj.live_link}}{\small[Live]}\BLOCK{ endif } &
  \BLOCK{ if proj.tech_stack }\textit{\small \VAR{proj.tech_stack | join(', ')}}\BLOCK{ endif } \\
\end{tabularx}
\vspace{2pt}
\BLOCK{ if proj.description }
\begin{itemize}
\BLOCK{ if proj.description is string }
  \item \small \VAR{proj.description}
\BLOCK{ else }
\BLOCK{ for bullet in proj.description }
  \item \small \VAR{bullet}
\BLOCK{ endfor }
\BLOCK{ endif }
\end{itemize}
\BLOCK{ endif }
\vspace{\VAR{item_vspace}}
\BLOCK{ endfor }
\vspace{\VAR{post_section_vspace}}
\BLOCK{ endif }

\end{tabularx}
\vspace{\VAR{item_vspace}}
\BLOCK{ endfor }
\vspace{\VAR{post_section_vspace}}
\BLOCK{ endif }

% ------ CERTIFICATIONS ------
\BLOCK{ if certifications }
\section{Certifications}
\begin{itemize}
\BLOCK{ for cert in certifications }
  \item \small
    \textbf{\BLOCK{ if cert.name }\VAR{cert.name}\BLOCK{ elif cert.title }\VAR{cert.title}\BLOCK{ else }Certification\BLOCK{ endif }}%
    \BLOCK{ if cert.issuer }\ \textit{--- \VAR{cert.issuer}}\BLOCK{ endif }%
    \BLOCK{ if cert.issue_date }\ {\footnotesize(\VAR{cert.issue_date})}\BLOCK{ endif }%
    \BLOCK{ if cert.link }\ \href{\VAR{cert.link}}{\footnotesize[View]}\BLOCK{ endif }%
\BLOCK{ endfor }
\end{itemize}
\vspace{\VAR{post_section_vspace}}
\BLOCK{ endif }

% ------ ACHIEVEMENTS ------
\BLOCK{ if achievements }
\section{Achievements}
\begin{itemize}
\BLOCK{ for ach in achievements }
  \item \small
    \BLOCK{ if ach.title }\textbf{\VAR{ach.title}}\BLOCK{ endif }%
    \BLOCK{ if ach.description }\ --- \VAR{ach.description}\BLOCK{ endif }%
\BLOCK{ endfor }
\end{itemize}
\vspace{\VAR{post_section_vspace}}
\BLOCK{ endif }

% ------ CAREER OBJECTIVE ------
\BLOCK{ if career_objective or objective }
\section{Career Objective}
\small\BLOCK{ if career_objective }\VAR{career_objective}\BLOCK{ else }\VAR{objective}\BLOCK{ endif }
\BLOCK{ endif }

\end{document}